<!-- GENERATED by scripts/gen_file_tree.py — do not edit by hand.
     The tree comes from `git ls-files`; the annotations from NOTES in that
     script. Add a file, then regenerate. -->

# File tree

Every tracked file, and what it is for. Generated from `git ls-files`, so it
cannot drift from the repository.

Three things live here at once — [the dataset](#the-study), [the guided
course](#the-guided-course), and [the talk](#the-talk) — and they are easier to
read separately than as one alphabetical list. The full tree is at the bottom.

## The study

The data and the reference implementations. This is the part you can use
without ever running an agent.

```
imbrave150_simulated.csv        501 patients, randomised          → teaching layer 1
hospitals/H01…H10.csv           1,800 patients, 3 EHR schemas     → teaching layer 2
hospitals_meta.csv              site catalogue — a separate file, on purpose
DATA_DICTIONARY.md              every column of layer 1
MULTIHOSPITAL_PSM.md            what mess layer 2 injects, and why
```

Generators run first, reference solutions run last; `make data && make analyze`
does both. See [`PIPELINE.md`](PIPELINE.md).

## The guided course

`live-demo/` — nine chapters, 32 missions, executed by an agent one at a time.
`README.md` there is the entry point and is written **for the agent**, not for
you. `verify/` holds the acceptance checks; `reference-run/` holds what a
finished run looked like; `workspace/` is the git-ignored sandbox.

## The talk

`slides/` — the recordings (`cast/`), the decks (`deck*.md` + `theme.css`), the
presenter's material (`SCRIPT.md`, `STRATEGY.md`) and the tooling that records,
builds and deploys all of it (`tools/`). `claude-demo/` is an older animated
deck telling the same story.

## Generated, not tracked

These appear once something runs. They are ignored by git, and listed here
because otherwise this map does not match the directory you are looking at.


| Path | Produced by |
|---|---|
| `live-demo/workspace/` | the sandbox the guided run writes into — **git-ignored, starts empty** |
| `live-demo/workspace/figures/` | KM curves, Love plot, PS overlap, forest, specification curve |
| `live-demo/workspace/manuscript/` | `manuscript.md`, `refs.bib`, `review_log.csv` |
| `live-demo/workspace/dist/` | the preprint PDF/DOCX from Chapter 08 |
| `slides/dist/` | the built PDFs |
| `slides/site/` | the assembled site (only `index.html` is tracked) |
| `dist/` | the release zips |
| `.venv/` | `make setup` |
| `imbrave150_pooled.csv` | `harmonize_hospitals.py` |
| `_answer_key_pooled.csv` | `generate_multihospital.py` — the truth, kept out of the way |
| `robustness_multiverse_results.csv` | `robustness_multiverse.py` |

## Every tracked file


| Path | What it is |
|---|---|
| `├── .claude/` | |
| `│   └── hooks/` | |
| `│       └── log-live-demo-read.sh` | logs which chapter the agent opened, and when |
| `├── .github/` | |
| `│   ├── ISSUE_TEMPLATE/` | |
| `│   │   ├── config.yml` | links shown above the issue form |
| `│   │   └── reproduction-report.yml` | for a run whose numbers came out elsewhere |
| `│   └── workflows/` | |
| `│       ├── deploy-talk.yml` | deploy the talk site to Cloudflare Pages |
| `│       ├── deploy.yml` | deploy the animated deck |
| `│       ├── docs.yml` | assert the generated docs are current |
| `│       ├── release-live-demo.yml` | package and release the course bundle |
| `│       └── slides.yml` | rebuild the decks, verify, release on a tag |
| `├── claude-demo/` | |
| `│   ├── assets/` | |
| `│   │   ├── deck.css` | its styling |
| `│   │   ├── deck.js` | slide navigation and animation |
| `│   │   ├── storyboard.js` | the six acts, as data |
| `│   │   └── studio.css` | the presenter view |
| `│   ├── figures/` | figures embedded in the animated deck (6 files) |
| `│   ├── index.html` | the animated HTML deck (predates the recording) |
| `│   └── README.md` | how to open and present it |
| `├── docs/` | |
| `│   ├── img/` | |
| `│   │   ├── forest.png` | the subgroup forest plot from the reference run |
| `│   │   ├── km-os.png` | matched-cohort survival curves from the reference run |
| `│   │   ├── multiverse.png` | the 120-specification curve from the reference run |
| `│   │   ├── recording-baseline.png` | real frame of the session at 9:20 — the baseline comparison |
| `│   │   ├── recording-prompt.png` | real frame at 6:18 — a prompt as it is submitted |
| `│   │   ├── recording-reasoning.png` | real frame at 17:55 — why IPTW and Cox disagree |
| `│   │   └── social-preview.png` | the repo card, 1280×640 — upload it under Settings → Social preview |
| `│   ├── FILE-TREE.md` | this file |
| `│   ├── PIPELINE.md` | what consumes what, and what it produces |
| `│   └── PROMPTS.md` | **the prompt list** — generated from the recorders |
| `├── hospitals/` | 10 per-hospital exports · 1,800 patients in 3 EHR schemas · **the only input** (10 files) |
| `├── live-demo/` | |
| `│   ├── reference-run/` | |
| `│   │   ├── results/` | the eight result tables from the reference run (8 files) |
| `│   │   ├── MANIFEST.tsv` | every output file: bytes, shape, SHA-256 |
| `│   │   └── README.md` | what one complete run produced, and how close yours should be |
| `│   ├── verify/` | one acceptance script per chapter + `preflight.py`, `run_all.py`, `_common.py` (12 files) |
| `│   ├── workspace/` | |
| `│   │   └── .gitignore` | the sandbox ignores itself — it starts empty on every clone |
| `│   ├── 00-setup.md` | Mission 0.1 — sandbox, and look at the raw files |
| `│   ├── 01-aggregate.md` | 1.1–1.3 — three dialects → 1,800 patients |
| `│   ├── 02-deconfound.md` | 2.1–2.4 — the unadjusted HR lies; matching fixes it |
| `│   ├── 03-survival.md` | 3.1–3.2 — Kaplan–Meier, log-rank, Cox, publishable curves |
| `│   ├── 04-subgroup.md` | 4.1–4.2 — forest plot: consistency, not winners |
| `│   ├── 05-robustness.md` | 5.1–5.3 — 120 analytic paths, then a different estimand |
| `│   ├── 06-manuscript.md` | 6.1–6.9 — Methods first, Title last, every citation Crossref-verified |
| `│   ├── 07-review.md` | 7.1–7.4 — four blind reviewers, then revise |
| `│   ├── 08-release.md` | 8.1–8.4 — pandoc/LaTeX → PDF, DOCX, preprint bundle |
| `│   ├── README.md` | **agent entry point** — pacing protocol, hard constraints, chapter index |
| `│   └── RUNBOOK.md` | the presenter's runbook |
| `├── scripts/` | |
| `│   ├── bundle-START-HERE.md` | the first thing in that bundle |
| `│   ├── extract_prompts.py` | regenerates `docs/PROMPTS.md` from the recorders |
| `│   ├── gen_file_tree.py` | regenerates `docs/FILE-TREE.md` |
| `│   ├── make-live-demo-bundle.sh` | the student download bundle |
| `│   ├── make-slides-bundle.sh` | the offline presenter zip |
| `│   ├── make_reference_run.py` | snapshots a finished run into `live-demo/reference-run/` |
| `│   └── make_screenshots.py` | regenerates `docs/img/` — real frames of the recording, and the repo card |
| `├── slides/` | |
| `│   ├── cast/` | |
| `│   │   ├── chapters.tsv` | chapter titles |
| `│   │   ├── RUNNING-ORDER.md` | what happens when, minute by minute |
| `│   │   ├── talk-full.cast` | the 64-minute version with the methodology kept in |
| `│   │   ├── talk-full.html` | its player |
| `│   │   ├── talk-markers.tsv` | the same for the long version |
| `│   │   ├── talk.cast` | an earlier short take |
| `│   │   ├── talk.html` | its player |
| `│   │   ├── walk-markers.tsv` | chapter marks, written while recording |
| `│   │   ├── walk.cast` | **the walkthrough recording** · 33 min · 22 chapters · 2.6 MB |
| `│   │   └── walk.html` | self-contained offline player for it |
| `│   ├── img/` | still frames from the recording, one per signpost (5 files) |
| `│   ├── recordings/` | per-segment MP4s of the same material — **kept deliberately** as a fallback for a room where the `.cast` player will not run (5 files) |
| `│   ├── site/` | |
| `│   │   └── index.html` | landing page; the rest of `site/` is assembled, not tracked |
| `│   ├── stage/` | |
| `│   │   ├── manuscript.md` | **the manuscript the recorded run wrote** |
| `│   │   ├── README.md` | what to put in place before presenting |
| `│   │   ├── review_log.csv` | the four reviewers' findings and what was done about them |
| `│   │   └── stage.sh` | copies them into the workspace so Chapter 07 runs standalone |
| `│   ├── talk/` | presenterm decks shown between prompts in the long recording (13 files) |
| `│   ├── tools/` | |
| `│   │   ├── add_markers.py` | splices chapter markers into a `.cast` |
| `│   │   ├── add_player_controls.py` | the projector UI: speed, chapters, click-to-pause, text zoom |
| `│   │   ├── build_notes.py` | renders the script and strategy card into the site |
| `│   │   ├── build_site.sh` | assembles `slides/site/` |
| `│   │   ├── check_overflow.py` | rasterises the PDFs and fails if a slide was clipped |
| `│   │   ├── deploy_site.sh` | deploys to Cloudflare Pages |
| `│   │   ├── make_backup_deck.py` | builds the backup deck from stills |
| `│   │   ├── record_cast.sh` | the earlier five-prompt take |
| `│   │   ├── record_segments.sh` | the per-segment recorder behind `slides/recordings/` |
| `│   │   ├── record_talk.sh` | records the long version |
| `│   │   ├── record_walkthrough.sh` | **records the walkthrough** — the prompt list lives here |
| `│   │   ├── script_timing.py` | measures how long the script takes to read aloud |
| `│   │   └── trim_cast.py` | trims dead air off the front, shifting markers |
| `│   ├── walkthrough/` | presenterm decks shown between prompts in the walkthrough (8 files) |
| `│   ├── .marprc.yml` | Marp configuration |
| `│   ├── deck-backup.md` | the same with recorded frames, for presenting without a browser |
| `│   ├── deck-full.md` | the 43-slide reference edition |
| `│   ├── deck.md` | the 12-slide talk deck |
| `│   ├── Makefile` | deck and recording targets |
| `│   ├── README.md` | how to build and present the deck |
| `│   ├── SCRIPT.md` | verbatim rehearsal script with stage directions |
| `│   ├── SPEAKER-NOTES.md` | the same, as notes |
| `│   ├── STRATEGY.md` | on-stage briefing: what is uncuttable, what to cut first |
| `│   └── theme.css` | `mono-academic` — monochrome, 18 px floor, Lucide icons |
| `├── .env.example` | template for Cloudflare credentials; the real `.env` is never committed |
| `├── .gitignore` | ignores the sandbox, the credentials and everything regenerable |
| `├── analyze_imbrave150.py` | reference solution · KM, log-rank, Cox (Python) |
| `├── analyze_imbrave150.R` | the same in R |
| `├── CITATION.cff` | how to cite this repository |
| `├── CLAUDE.md` | instructions for an agent opening this repo — the stage protocol |
| `├── CODE_OF_CONDUCT.md` | |
| `├── CONTRIBUTING.md` | what a useful contribution looks like here |
| `├── DATA_DICTIONARY.md` | every column of the randomised dataset: type, units, encoding |
| `├── generate_imbrave150.py` | the simulator for the randomised trial |
| `├── generate_multihospital.py` | confounds it, splits it into 10 sites, dialect-ises it |
| `├── harmonize_hospitals.py` | reference solution · 10 dialects → one table |
| `├── hospitals_meta.csv` | site catalogue: type, region, EHR vendor, size. **Not** in the patient files. |
| `├── imbrave150_simulated.csv` | 501 patients, randomised — teaching layer 1 |
| `├── LICENSE` | MIT, plus the note that every dataset is synthetic |
| `├── Makefile` | `setup` · `data` · `analyze` · `figure` |
| `├── MULTIHOSPITAL_PSM.md` | the real-world layer: what mess was injected, and why |
| `├── psm_imbrave150.py` | reference solution · matching, balance, matched survival |
| `├── psm_imbrave150.R` | the same in R |
| `├── README.md` | start here |
| `├── requirements.txt` | pinned hard; `lifelines==0.30.0` is load-bearing |
| `├── robustness_multiverse.png` | the specification curve |
| `├── robustness_multiverse.py` | reference solution · 120 specifications |
| `├── search_seed.py` | how `SEED=48` was chosen |
| `└── tmle_demo.py` | reference solution · G-comp, IPTW, AIPW, TMLE vs the true DGP |

---

175 tracked files. Regenerate with `python3 scripts/gen_file_tree.py`.
