# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Derived / regenerable artifacts (produced by the scripts)
# answer key — regenerated by generate_multihospital.py
_answer_key_pooled.csv
# output of harmonize_hospitals.py
imbrave150_pooled.csv
robustness_multiverse_results.csv
# redundant bundle of hospitals/
hospitals_10sites.zip

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Cloudflare credentials. Ignored globally too, but a global gitignore does not
# travel with a clone — this is the copy that matters.
.env
.env.*
!.env.example

# Cloudflare Pages / wrangler local cache & build artifacts
.wrangler/

# Claude Code local settings + demo timing log
.claude/settings.local.json
.claude/live-demo-timing.log

# live-demo 下載包（由 scripts/make-live-demo-bundle.sh 產生）
dist/

# Built slide decks
slides/dist/

# The public site is assembled from the recordings and the built PDFs by
# slides/tools/build_site.sh; only its landing page is source.
slides/site/*
!slides/site/index.html
